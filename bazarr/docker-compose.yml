services:
  bazarr:
    container_name: ${NAME}
    image: lscr.io/linuxserver/bazarr:latest
    restart: unless-stopped
    ports:
      - ${PORT}:6767
    volumes:
      - ${VOLUME_CONFIG}:/config
      - ${VOLUME_DATA}:/data
    env_file:
      - .env
