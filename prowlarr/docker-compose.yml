services:
  prowlarr:
    container_name: ${NAME}
    image: lscr.io/linuxserver/prowlarr:latest
    restart: unless-stopped
    ports:
      - ${PORT}:9696
    volumes:
      - ${VOLUME_CONFIG}:/config
    env_file:
      - .env
