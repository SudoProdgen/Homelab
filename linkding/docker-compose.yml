services:
  linkding:
    container_name: linkding
    image: sissbruecker/linkding:latest
    restart: unless-stopped
    ports:
      - 9091:9090
    volumes:
      - /srv/containers/linkding/data:/etc/linkding/data
    env_file:
      - .env
